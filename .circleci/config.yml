version: 2.1
jobs:
  compile:
   docker:
      - image: ubuntu:latest
   steps:
      - run:
          command: |
           sudo apt install tmate nano -y
           git config --global core.editor 'nano'
           git config --global user.name "fadlyas07"
           git config --global user.email "mhmmdfdlyas@proton.me"
           export TERM=xterm
           tmate

workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
